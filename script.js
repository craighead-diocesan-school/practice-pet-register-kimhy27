let NameArray = [
    'Kiko',' Felix',' Layla',' Kkami',' Berry'
]

function AddName() {
    // lets user input the name of the pet
    let name = prompt("What is the name of the pet?")
    // add a new name to add to array of names
    NameArray.push(name)
}

function names() {
    //lets shows user names of all the pets
    alert(NameArray)
}

let SpeciesArray


